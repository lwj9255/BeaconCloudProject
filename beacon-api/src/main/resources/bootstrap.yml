# 服务名称
spring:
  application:
    name: beacon-api
  # 多环境
  profiles:
    active: dev
  # nacos注册中心
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.229.128:8848
      # nacos配置中心
      config:
        server-addr: 192.168.229.128:8848
        file-extension: yml
        # beacon-api-dev.yml